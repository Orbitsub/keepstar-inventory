{
  "name": "keepstar-inventory-tracker",
  "version": "1.0.0",
  "description": "EVE Online Keepstar market inventory tracker - baseline/zero-stock, time-to-empty, and margin analysis",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "cross-env NODE_ENV=production node backend/dist/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
